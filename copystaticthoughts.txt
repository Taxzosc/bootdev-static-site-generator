write a recursive function that copies all the contents from a source directory to a destination directory(static to public)
delete all contents of the destination directory(public). inside the recurse i assume
then copy all files, and sub directories, nested files etc.. 