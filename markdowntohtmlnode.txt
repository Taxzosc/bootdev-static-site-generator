just writing thoughts of how this shit is functioning
"based on thEtype of block, create a new htmlnode with the proper data"
what is the proper data for a block. if its a parent node it needs no tags, but how do i figure out if its a parent node..
if i split up a markdown block at all double newlines(markdown to blocks), what do i have there? i have a list of blocks.
i can then assign what is in those blocks with the block to blocktype function.
these would be just straight htmlnodes with the proper data. this will only be the block markdown, not inline. inline can be inside all of the blocks
so the block element can contain inline elements. it affects visuals mostly. like bullet points, or ordered list, quote and code all makes different
visuals in html.
so with the current block in the loop, i need to find the type of block, block to blocktype.
save that in a variable, then run it through a function to find if it has child elements, which all will have, which means this is a parent node.
even code which can only contain a text file, is a parent node. so it needs to be a parentnode(blocktoblocktype(), children, and if it has props?)
thats the result.

so we find the blocktype, save to variable. the block type only matters to how it will look like.
then we make if statements, if blocktype==code, paragraph, orderedlist etc.
find what kind of children is inside of the text. except if its code block. it should  be a textnode.
create a helperfunction to find the children?
parent_node = prentnode(tag, children in the form of a function(blocktext),props?)
then after all that, return parentnode(div,parent_node)

im not sure about the props thing... if given it will be used i guess?
props is taken care of if there is props, as we will call to_html on leafnodes anyways, which is where props come in if there is any.

for the helper function for finding what children is in the block. some blocks can have block children in it. atleast in regular markdown, maybe not
in this simplified one.. im going to assume not. asked boots! not in scope. maybe ill try to write that later when im done.

helper function. it needs to take the text and return a list of htmlnodes for all the inline markdown.
i can use the text_to_textNodes to make a list of inline textnodes. then i can loop over all those with the .to_html function.
but i do think i need to remove whichever header,code,list symbol before i run this helperfunction. which means i need to STRIP the type of header/code/whatever from the block of text?
else the block is only "### header" instead of h1-6 header /h1-6.. i might need more helper functions to sort that out for EACH TYPE..

HEADER:
i will need a function to find out how many # are in the header, to find out which tag it should have. also to strip said block of the #.
.strip"#" will remove all # from the beginning and end of a string. if i add a space to the # it will strip that too.
how do i find out how many # are in the beginning of the string.
i can use regex. extract the amount of # at the beginning of the header
re.match(pattern, string)
since headers only have # at the beginning, i can Lstrip("# "), save as variable, len(original) - len(new) = total -1 because of the space.
i searched google to figure that out, which gave me ai response.

I DO NOT NEED TO CONVERT TO HTML YET? YES, NOT YET.

for code. i need to remember, theres a difference between a code BLOCK and a code "line". ```\n code``` vs `code`.. i think. not in this project i think.

for list and unordered list i need the helper function to make a list of leaf nodes with the tag "il", i think i can do this with just one function,
as the differences between them is mostly aestethical, so the difference is in the parent node tag "ul" and "ol".
i first need to separate the block itself into a list of "objects".. so maybe 2 different functions

note: i might need to find a way to keep a \n at the end of the last item in the list. no i think that is handled by the html.

for ordered list i need a way to split and remove the beginning number, no matter what it is. as long as the starting number is 1,
it doesnt matter what the next numbers are. the ol tag followed by il tag takes care of the rest. the thing that makes it a ordered list,
is the punctuation. it HAS to have the . how do i take only the stuff thats after the . ? .... theres also the newline at the end.
so what signifies a ordered list is that the first number is always 1. actually when i test on jotbird i can start with any number followed by a .
though in  the previous task we got the "Every line in an ordered list block must start with a number followed by a . character and a space. The number must start at 1 and increment by 1 for each line."
which signifies that in this project, an ordered list is from 1 and up, incremented by 1 always. 
count 1, take whatever is between 1. and the "\n", set it as a parentnode, with children as run the inline markdown check, and somehow remove this section
from the block then count +1.. recursive?
making a test to see what the program sees.


quoteblocks are weird. if i write 
"""
> this is a quote block
> with **two** lines
"""
the output of my program is
<div><blockquote>this is a quote block\n> with two lines</blockquote></div>
I just checked https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/blockquote
it should be <blockquote><p></p</blockquote>? but why is my helper function not doing that..
not in this project.
however, the >'s at the beginning should be handled. so the result would be <div><blockquote>this is a quote block with <b>two</b> lines</blockquote></div>
